connector.name=mysql
# Database must not appear in the JDBC URL (use schema `demo` in SQL: mysql.demo.<table>).
connection-url=jdbc:mysql://mysql:3306?allowPublicKeyRetrieval=true
connection-user=trino_demo
connection-password=trino_demo
