apiVersion: v1
kind: Service
metadata:
  name: gitops-frontend-service
  labels:
    app: gitops-frontend
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: gitops-frontend
