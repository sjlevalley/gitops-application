apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: gitops-frontend-base

resources:
- deployment.yaml
- service.yaml

commonLabels:
  app.kubernetes.io/name: gitops-frontend
  app.kubernetes.io/component: frontend
  app.kubernetes.io/part-of: gitops-application

images:
- name: gitops-frontend
  newTag: latest
